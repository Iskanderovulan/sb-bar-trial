import React from 'react';
import './Button.css'

const Button = () => {
    return (
        <div className={'button__parent'}>
<button className={'button'}>
    Загрузить еще
</button>
        </div>
    );
};

export default Button;